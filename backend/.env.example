DATABASE_URL="postgresql://usuario:password@localhost:5432/tienda_siber?schema=public"
# IMPORTANTE: genera un secreto fuerte con: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# El backend rechaza arrancar en producción si JWT_SECRET es débil, vacío o el placeholder.
JWT_SECRET="cambia_esto_por_un_secret_seguro"
JWT_EXPIRES_IN="7d"
PORT=4000
SUPABASE_URL=""
SUPABASE_SERVICE_ROLE_KEY=""
